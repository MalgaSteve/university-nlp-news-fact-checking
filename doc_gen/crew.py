python
# crew.py 
def kickoff(self):
   # Execute the process sequentially.
   result = execute_process(topic=self.topic)
return format_result(result)

libraries_used ['execute_process', 'format_result'}
```
